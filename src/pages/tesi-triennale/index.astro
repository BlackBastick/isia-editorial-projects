---
import EntityArchive from "../../components/EntityArchive.astro";
import SeoHead from "../../components/SeoHead.astro";
import SiteHeader from "../../components/SiteHeader.astro";
import { projects } from "../../data/projects";
import "../../styles/global.css";

const tesiProjects = projects.filter((project) => project.subject?.toLowerCase().includes("tesi triennale"));
---

<!doctype html>
<html lang="it">
  <head>
    <SeoHead title="Tesi triennale — ISIA Urbino Editorial Projects" description="Tesi triennali nell’archivio ISIA Urbino Editorial Projects." pathname="/tesi-triennale/" />
  </head>
  <body>
    <SiteHeader />
    <EntityArchive label="Sezione" title="Tesi triennale" projects={tesiProjects} />
  </body>
</html>
